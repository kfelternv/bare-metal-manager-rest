# OpenAPI Generator Ignore
# Prevent overwriting custom files during regeneration.

# We manage our own go.mod at the repo root
go.mod
go.sum

# CI files we don't need
.travis.yml
git_push.sh
.gitignore
